<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
</head>
<body>
    <h1><%= val %></h1>
    <!-- <ul>
        <% for(let i = 1;i < 11; i++) { %>
            <li>
                <%= i %>
            </li>
        <% } %>
    </ul>
    <% if (name === 'maini'){ %>
        <h1>the person is in the contact list</h1>
    <% } else { %>
        <h1>No the person is in the contact list</h1>
    <% } %> -->

    <% switch (exp) {
        case 'Cookies': %>
          <h1>Cookies cost 100 rupees </h1>
        <% break
        case 'BreadCrumb': %>
            <h1>BreadCrumb cost 200 rupees</h1>
        <% break    
        default:
            %>
          <h1><%= exp %> is not available right now </h1>
    <% } %>
</body>
</html>
